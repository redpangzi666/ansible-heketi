{
  "clusters": [
    {
      "nodes": [
{% for host in groups['node_add'] %}
        {
          "node": {
            "hostnames": {
              "manage": [
                "{{ host }}"
              ],
              "storage": [
                "{{ host }}"
              ]
            },
            "zone": 1
          },
          "devices": {{ hostvars[host].disk.split(',')|to_json }}
        }{% if not loop.last %},{% endif %}{% endfor %}
      ]
    }
  ]
}

